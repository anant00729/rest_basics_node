// const serverCallback = (PORT) => {
//     console.log(`The server is running on ${PORT}`)
// }

// module.exports = {serverCallback}


exports.serverCallback = (PORT) => {
    console.log(`The server is running on ${PORT}`)
}

// exports.add = (a,b) => {
//     console.log(`The server is running on ${a + b}`)
//     return a + b
// }

// module.exports = {serverCallback,add}


//module.exports



